import React from 'react'

export default function NewSvg(svgAtrributes) {

    console.log(svgAtrributes)
    
  return (
    <svg 
        // height={height ? height : null}
        // width={width ? width : null}
        // viewBox={viewBox ? viewBox : null}
        // xmlns={xmlns ? xmlns : null}
    >
    </svg>
  )
}
